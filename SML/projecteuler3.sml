fun esieve x = 
  let 
    val sieve = List.tabulate(floor (Math.sqrt(Real.fromInt(x))), fn x => false)
    fun markPrimes (l::ls) n index = if (index mod n) = 0

    fun makeSieve [] = 
  in

  end;
